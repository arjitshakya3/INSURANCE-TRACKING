name: insurance_renewal_advisor
description: Offline-first Android app for vehicle insurance renewal advisors.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.1.7
  flutter_local_notifications: ^17.2.4
  intl: ^0.19.0
  path: ^1.9.0
  path_provider: ^2.1.5
  pdf: ^3.11.1
  share_plus: ^10.1.4
  shared_preferences: ^2.3.5
  sqflite: ^2.4.1
  timezone: ^0.9.4
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
